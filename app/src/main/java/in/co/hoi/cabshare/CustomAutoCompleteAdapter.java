package in.co.hoi.cabshare;

/**
 * Created by lenovo pc on 20-05-2015.
 */





    public class CustomAutoCompleteAdapter {}
     /*   private final Context context;
        private final String[] values;

        public CustomAutoCompleteAdapter(Context context, String[] values) {
            super(context, R.layout.autocomplete_list, values);
            this.context = context;
            this.values = values;
        }

        @Override
        public View getView(int position, View convertView, ViewGroup parent) {
            LayoutInflater inflater = (LayoutInflater) context
                    .getSystemService(Context.LAYOUT_INFLATER_SERVICE);
            View rowView = inflater.inflate(R.layout.autocomplete_list, parent, false);
            TextView textView = (TextView) rowView.findViewById(R.id.label);
            ImageView imageView = (ImageView) rowView.findViewById(R.id.icon);
            textView.setText(values[position]);
            // change the icon for Windows and iPhone
            String s = values[position];
            if (s.startsWith("iPhone")) {
                imageView.setImageResource(R.drawable.no);
            } else {
                imageView.setImageResource(R.drawable.ok);
            }

            return rowView;
        }
    }

*/